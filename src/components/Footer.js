import React from 'react';

function Footer() {

    return (
        <div>
            <footer className="footer customFooter dark bg-dark">
                <div className="container"><span className="text-muted">Made with Bootstrap</span></div>
            </footer>
        </div>
    )
};

export default Footer;